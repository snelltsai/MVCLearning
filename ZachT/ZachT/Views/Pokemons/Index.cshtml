@model IEnumerable<ZachT.Models.Pokemon>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.PokeName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.PokeName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.PokeID }) |
            @Html.ActionLink("Details", "Details", new { id=item.PokeID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.PokeID })
        </td>
    </tr>
}

</table>
